[project]
name = "formulario-construcao"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
    "flask-limiter>=3.5.0",
    "bleach>=6.1.0",
    "reportlab>=4.1.0",
    "python-dotenv>=1.0.0",
    "gunicorn>=21.2.0",
]

[tool.vercel]
entrypoint = "run:app"